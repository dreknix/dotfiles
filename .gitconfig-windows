# This is Git's windows configuration file.
#

[core]
  autocrlf = false
  sshcommand = C:/Windows/System32/OpenSSH/ssh.exe
  pager =

#[merge]
#  tool = vimdiff

#[pager]
#  diff = diff-so-fancy | less --tabs=4 -RFX
#  show = diff-so-fancy | less --tabs=4 -RFX

#[credential]
#	 helper = manager-core
